<?xml version='1.0' encoding='utf-8'?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="close.tm.plus">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>

    <uses-permission android:name="android.permission.REBOOT"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_tm"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:resizeableActivity="true">

        <activity
            android:name="FileManagerActivity"
            android:label="@string/app_name">

            <intent-filter>

                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>

        </activity>

        <meta-data
            android:name="android.max_aspect"
            android:value="4.0"/>

        <activity android:name="MainActivity"/>

        <activity
            android:name="close.tm.plus.ThanksActivity"
            android:exported="true"/>

        <activity android:name="close.tm.plus.TextEditorActivity"/>

        <activity android:name="close.tm.plus.ImageViewerActivity"/>

        <activity android:name="close.tm.plus.ZipUtils"/>

    </application>

</manifest>